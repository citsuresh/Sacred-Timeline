# Sacred Timeline: Agent Instructions

## User Preferences
- **Identity**: Suresh (citsuresh@rediffmail.com)
- **Commit Rule**: NEVER auto-commit. Always wait for explicit user approval.
- **Commit vs. Push**: NEVER push unless the user explicitly uses the word "push". 
- **Auto-Deploy**: Automatically deploy to the connected phone after implementation.

## Handover Instructions
1. **Context First**: At the start of every session, read this file, `docs/PROJECT_STATE.md` (for progress), and `docs/CODE_SUMMARY.md` (for architecture).
2. **Localization First**: Every user-facing string MUST be externalized. Maintain both English (`values/`) and Tamil (`values-ta/`) translations.
3. **Architecture**: Ensure all new UI follows the established Material 3 / 24h timeline pattern. Consult `docs/DESIGN_DECISIONS.md` for existing patterns.
4. **Verification**: Verify git identity before any commit.

## Session Shortcuts

### /begin-session
"Please read `.agent-instructions.md`, `docs/PROJECT_STATE.md`, `docs/CODE_SUMMARY.md`, and `docs/DESIGN_DECISIONS.md` immediately. Adopt the user identity (Suresh), acknowledge the rules, and provide a 1-sentence summary of the current project state to confirm readiness."

### /end-session
"Before we finish, please review all `.md` files in the `docs/` folder. Update `docs/PROJECT_STATE.md` with the changes in this session, and update `docs/CODE_SUMMARY.md` or `docs/DESIGN_DECISIONS.md` if any structural or architectural changes were made. Provide a final summary and check for pending commits."
