# Sacred Timeline: Agent Instructions

## User Preferences
- **Identity**: Suresh (citsuresh@rediffmail.com)
- **Commit Rule**: NEVER auto-commit. Always wait for explicit user approval and a specific commit command after a review.
- **Commit vs. Push**: Commit and Push are separate operations. NEVER push code to the remote repository unless the user explicitly uses the word "push". If the user says "commit this", only perform `git add` and `git commit`.
- **Auto-Deploy**: Once a plan is implemented and verified, automatically deploy the application to the connected phone so the user can test and give feedback. DO NOT automatically verify changes using screenshots/UI state, user will provide feedback after testing. verify only when explicitly requested by the user.

## Project Context
- **Name**: Sacred Timeline (Panchangam Timings Visualizer)
- **Repo**: https://github.com/USERNAME/Sacred-Timeline
- **Stack**: Android 14, Kotlin, Jetpack Compose, Navigation 3, Material 3, Glance (Widget), WorkManager.
- **Current State**: Stable 24h timeline with multi-day navigation and background preloading. UI features screenshot-accurate shaded color coding and high-contrast double borders. **Modular Architecture**: Refactored `TimelineDashboard.kt` into dedicated components (`TimingCard`, `TimelineCore`, `DateDials`, etc.) for stability and maintainability. **Persistent Cache**: Integrated disk-based JSON caching (shared between App and Widget) for instant launch, offline support, and zero redundant network calls. **Advanced Settings**: Full implementation of user preferences (24h format, default launch view, zoom levels, and NOW line customization). **Column Management**: Supports visibility and reordering for both App (Composite) and Widget views. **Robust Location System**: Supports GPS auto-detect with last-known fallback and manual city search with geocoding/disambiguation. **Widget Reliability**: Features expedited transition updates, shared cache priority, and configurable refresh intervals. **Multi-Language Support**: Full English and Tamil support implemented using string resources and `AppCompatDelegate`. Users can toggle language in Settings, affecting the app and widget globally. **Stability**: Thread-safe cache access (Mutex) and navigation backstack protections implemented.

## Handover Instructions
1. Read this file at the start of every session.
2. Verify git identity before any commit.
3. Ensure all new UI follows the established Material 3 / 24h timeline pattern.
4. Consult the `docs/` directory for a deep dive into the project's architecture, design decisions, and current state.

## Session Shortcuts

### /begin-session
"Please read `.agent-instructions.md` immediately. Adopt the user identity (Suresh/citsuresh@rediffmail.com), acknowledge the 'No Auto-Commit' rule, and provide a brief summary of the current project state to confirm you are ready."

### /end-session
"Before we finish, please update the 'Current State' section in `.agent-instructions.md` to reflect our latest progress. Then, provide a final summary of what we accomplished in this session and check if there are any pending files to commit (remembering to ask for permission first)."